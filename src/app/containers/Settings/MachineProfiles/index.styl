@import '../../../styles/gruvbox';
@import '../form';

.table-toolbar {
    position: relative;
    background-color: $bg;
    border-bottom: 1px solid $darker_gray;
    padding: 8px 16px;

    clearfix();
}
